<configuration>
    <system.webServer>
        <handlers>
            <!-- Specifies that index.js should be handled by IISNode -->
            <add name="iisnode" path="index.js" verb="*" modules="iisnode" />
        </handlers>
    </system.webServer>
</configuration>
